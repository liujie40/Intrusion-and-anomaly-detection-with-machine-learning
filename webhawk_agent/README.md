
# Webhawk/Catch Agent

Agent to use in your endpoints. 

## About

This page contains infomations about how to use Webhawk agent on your endpoints. 

### Using a Python virtual env

```shell
python -m venv webhawk_agent
source webhawk_agent/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```
